{
  "name": "liquidation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"client,server\" -c \"cyan,magenta\" \"npm run dev --prefix client\" \"npm run start:dev --prefix server\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "client": "npm run dev --prefix client",
    "server": "npm run start:dev --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
